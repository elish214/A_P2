<UserControl x:Class="GUI.controls.MazeCreationMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GUI.controls"
             mc:Ignorable="d" 
             d:DesignHeight="110" d:DesignWidth="300">
    <Grid TextBlock.FontSize="14" >
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <TextBlock Margin="22,8,8,8" Height="25">Maze Name:</TextBlock>
        <TextBox x:Name="txtName" Grid.Column="1" Text="{Binding MazeName}" Margin="12,8,20,8"/>
        <TextBlock Grid.Row="1" Margin="22,8,8,8" Height="25">Maze Rows:</TextBlock>
        <TextBox x:Name="txtRows" Grid.Row="1" Grid.Column="1" Text="{Binding MazeRows}" Margin="12,8,20,8"/>
        <TextBlock Grid.Row="2" Margin="22,8,8,8" Height="25">Maze Columns:</TextBlock>
        <TextBox x:Name="txtCols" Grid.Row="2" Grid.Column="1" Text="{Binding MazeCols}" Margin="12,8,20,8"/>
    </Grid>
</UserControl>
